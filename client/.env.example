# Absolute origin of the backend API (the backend Vercel project's URL).
# Leave unset for local dev — Vite proxies "/api" to http://localhost:8000.
VITE_API_URL=https://your-backend-project.vercel.app
